---

---

<div class="my-16 mx-auto outline outline-1 outline-surface2 p-16 shadow-2xl rounded-md flex flex-row gap-10" data-tilt data-tilt-max="10" data-tilt-scale="1.1" data-tilt-glare data-tilt-max-glare="0.05" id="card">
    <img src="/avatar.png" alt="hex4 avatar" class="size-64 rounded-full">
    <div>
        <h1 class="text-5xl text-text font-bold"  id="name" data-value="hex⁴">hex⁴</h1>
        <h1 class="text-lg text-overlay0" data-value="making cool stuff">game + web dev, pixel artist, open source enthusiast</h1>
        <br>
        <p class="text-text font-sans text-xl whitespace-pre-line">also trying to make the internet fun again.</p>
        <br>
        <a class="fa-brands fa-github text-text size-10 fa-2x hover:text-peach transition-all" href="https://github.com/Hex-4"></a>
        <a class="fa-brands fa-itch-io text-text size-10 fa-2x hover:text-peach transition-all" href="https://the-dimaverse.itch.io/"></a>
        <a class="fa-brands fa-x-twitter text-text size-10 fa-2x hover:text-peach transition-all" href="https://x.com/hex4_makes"></a>
        <a class="fa-solid fa-fire text-text size-10 fa-2x hover:text-peach transition-all" href="https://scrapbook.hackclub.com/hex4"></a>
    </div>

</div>
<script src="https://kit.fontawesome.com/f0fceaee00.js" crossorigin="anonymous"></script>
<script>
    import VanillaTilt from 'vanilla-tilt';

    // Modified code from Hyperplexed. https://codepen.io/Hyperplexed/full/rNrJgrd
    const letters = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz1234567890!@#$%^&*()";

    let interval = null;

    document.getElementById("card")
    .onmouseover = event => {  
    let iteration = 0;
    let text = document.getElementById("name")
    
    clearInterval(interval);
    
    interval = setInterval(() => {
        text.innerText = text.innerText
        .split("")
        .map((letter, index) => {
            if(index < iteration) {
            return text.dataset.value[index];
            }
        
            return letters[Math.floor(Math.random() * 72)]
        })
        .join("");
        
        if(iteration >= text.dataset.value.length){ 
        clearInterval(interval);
        }
        
        iteration += 1 / 4;
    }, 75);
    }
</script>